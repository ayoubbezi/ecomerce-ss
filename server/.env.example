# Database Configuration
# Railway will provide DATABASE_URL, but these are used for local development
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=anonstore
DB_DIALECT=mysql

# Port (Railway will set PORT automatically)
PORT=5000

# NODE_ENV (set to production on Railway)
NODE_ENV=development

# JWT Secret (if you implement authentication)
# JWT_SECRET=your_jwt_secret_here 